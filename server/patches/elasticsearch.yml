indices:
  memory:
    index_buffer_size:
      30%
thread_pool:
  #for search operation
  search:
    size: 9
    queue_size: 200

  #for bulk operations
  bulk:
    size: 9
    queue_size: 1000

  #for indexing operations
  index:
    size: 9
    queue_size: 1000
